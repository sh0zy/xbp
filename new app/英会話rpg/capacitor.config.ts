import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.kota.englishquestrpg",
  appName: "English Quest RPG",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
